git checkout -b fix/tw-config
cat > tailwind.config.js <<'EOF'
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./**/*.{html,js,ts}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
EOF
git add tailwind.config.js
git commit -m "tailwind: de-duplicate config and correct content globs"
git push -u origin fix/tw-config
