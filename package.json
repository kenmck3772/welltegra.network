{
  "name": "welltegra-pa-course-tests",
  "version": "1.0.0",
  "description": "Visual tests for P&A Course interactive elements",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:video": "playwright test tests/video-course-flow.spec.js --headed",
    "test:interactive": "playwright test tests/pa-course.spec.js --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
