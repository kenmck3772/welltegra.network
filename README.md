# WellTegra — Revert Start (v22)

This is your uploaded HTML restored as `index.html` for a clean starting point.

## Deploy (GitHub Pages)
1. Replace your repo’s `index.html` in the root with this one.
2. Commit & push to `main`.
3. Keep your existing `CNAME` file if you use a custom domain.
4. Hard refresh once after deploy.

— Generated fresh ZIP (v22)
